# Contract Test: GET /api/auth/providers

- Expected response shape: `{ providers: string[] }`
- Status code: 200
- Notes: List should include configured providers; exact values depend on runtime configuration.
