export const dynamic = 'force-dynamic';

export default async function CoursesPage() {
  return (
    <div data-testid='course-overview'>
      <h1>Courses - Minimal Test</h1>
      <p>If you see this, the page renders!</p>
    </div>
  );
}
