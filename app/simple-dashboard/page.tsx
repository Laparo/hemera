import { Typography } from '@mui/material';

export default function SimpleDashboard() {
  return (
    <div>
      <Typography variant='h1'>Simple Dashboard</Typography>
      <Typography>This route should work without any auth</Typography>
    </div>
  );
}
