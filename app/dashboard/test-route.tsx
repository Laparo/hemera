import { Typography } from '@mui/material';

export default function TestPage() {
  return (
    <div>
      <Typography variant='h1'>Test Dashboard Route</Typography>
      <Typography>This is a test to verify route resolution works</Typography>
    </div>
  );
}
